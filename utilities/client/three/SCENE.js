import { 
	Scene, 
	Color, 
	AxesHelper,
	// FogExp2, 
	// BoxBufferGeometry,
	// MeshLambertMaterial,
	// Mesh,
	// DoubleSide,
} from "https://cdnjs.cloudflare.com/ajax/libs/three.js/r134/three.module.js"
// '/three-patch/build/three.module.js'

const scene = new Scene()

scene.needs_render = false

export default scene
